# ChatGPT Adapter

Goal:
- Standardize usage of the same modular context system in sessions outside the IDE.

Load instructions for ChatGPT:
1. Use `docs/context/README.md` as the index.
2. Include modules `00` to `10` based on task type.
3. For coding tasks, prioritize `03`, `04`, `06`, `07`, and `08`.
4. Keep `10_session_history.md` updated at the end of each session.

Language policy:
- Internal context and instruction files must stay in English.
- The root `README.md` remains in Spanish.
