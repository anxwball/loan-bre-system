# Top-level CI dependencies source file.
# Regenerate lock after edits with the same Python version used in CI (currently 3.13).
# This file includes compiled packages (for example numpy and pandas), so avoid using a different
# Python version when compiling hashes or selecting wheels.
# python -m piptools compile --resolver=backtracking --generate-hashes --output-file requirements-ci.lock.txt requirements-ci.txt
pytest==9.0.2
numpy==2.4.4
pandas==3.0.2
SQLAlchemy==2.0.48
fastapi==0.135.3
httpx==0.28.1
PyJWT==2.12.0
passlib[bcrypt]==1.7.4
bcrypt<5.0.0
python-dotenv==1.2.2
